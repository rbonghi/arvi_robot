<launch>
  <!-- ROSBRIDGE
  <include file="$(find rosbridge_server)/launch/rosbridge_websocket.launch" > 
     <arg name="port" value="9090"/>
  </include>
  -->
  
  <!-- GRAPHIC ROBOT ARVI -->
  <param name = "robot_description" command = "cat $(find arvi_robot)/arvi.urdf" />

  <!-- MOTION CONTROLLER -->
<!--
  <group ns="robot">
  	<rosparam command="load" file="$(find arvi_robot)/arvi_motors.yaml"/>
		<node name="bridge_robot" pkg="serial_bridge" type="serial_bridge_node" args="/dev/ttyUSB0"/>
  </group>
  
<node name="topic_tools_relay" pkg="topic_tools" type="relay" args="/robot/joint_states joint_states"/>
-->
  <!-- POLOLU SERVO -->
  
  <rosparam command="load" file="$(find arvi_robot)/maestro.yaml" ns="maestro_node" />
  <node name="maestro_node" pkg="maestro_node" type="maestro_node" args="/dev/ttyACM0">
    <remap from = "maestro_node/joint_states" to = "joint_states" />
  </node>

</launch>
